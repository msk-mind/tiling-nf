nextflow.enable.moduleBinaries = true

params {
    dremio {
        hostname = 'tlvidreamcord1'
        port = 32010
        scheme = 'grpc+tcp'
        space = 'OCRA.archive'
        table = 'matched_QC_case_breakdown'
    }
}
